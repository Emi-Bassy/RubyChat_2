export default function PrevProblemDisplay() {
    const handlePrevProblem = () => {
        // 前のプログラミング課題を取得するロジックを追加
    };

    return (
        <button onClick={handlePrevProblem}>前の課題</button>
    );
}