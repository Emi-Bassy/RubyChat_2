export default function NextProblemDisplay() {
    const handleNextProblem = () => {
      // 次のプログラミング課題を取得するロジックを追加
    };
  
    return (
      <button onClick={handleNextProblem}>次の課題</button>
    );
  }
  